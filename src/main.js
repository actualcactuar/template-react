import './main.css';

import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

const node = document.getElementById('app');

// warning here is fine
ReactDOM.render(<App />, node);
